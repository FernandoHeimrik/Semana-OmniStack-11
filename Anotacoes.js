/**
*	rotas
*/
npm install react-router-dom

/**
*	client Http
*/
npm install axios

/**
*	javascript format
*/
Intl

/**
*	passa uma funcao
*/
onClick={() => handleDeleteIncident(incident.id)}

/**
*	passa o retorno de uma funcao
*/
onClick={handleDeleteIncident(incident.id)}